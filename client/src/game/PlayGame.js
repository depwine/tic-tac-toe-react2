import { useContext } from "react"
import { UserContext } from "../Backend/UserContext"


const PlayGame = () => {

    const {turn, setTurn, setReason} = useContext(UserContext)
    

    // win condition

    // if game not over 

    return (
        null
    )


}

export default PlayGame